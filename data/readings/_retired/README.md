# Retired readings — moved out of the sweep, not deleted

Moved here 2026-09-19 during the v0.3 hygiene pass. Nothing is destroyed (I8); these are
out of `drafts/` so the admin-surface sweep and any `drafts/*.json` glob stop seeing them.

- `rdg_mrsac7qy2ceb.json` — "zz Photo grammar verification (safe to delete)". Test artifact,
  self-labelled disposable.
- `rdg_ms51urghwerb.json` — "Supreme Court decision: Federal Reserve cutout". ZERO responses —
  an empty shell. No authored content to lose; re-author from scratch if the event is wanted.

**Note:** these may still exist in the browser store on github.io. Moving the file does not
clear the store — remove them from the admin surface if they show up there.
